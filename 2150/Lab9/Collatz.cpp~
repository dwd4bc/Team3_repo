#include<iostream>
using namespace std;

int collatz (int x){
  int steps = 0;
  if(x==1)
    return steps;
  if(x%2=0){
    collatz(x/2);
    steps++;
  }
  else{
      collatz(3x+1);
      steps++;
  }
}
int main(){
  cout <<collatz(13)<<endl;
}
